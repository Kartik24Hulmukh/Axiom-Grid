# PowerPoint oracle fixtures

`test_image.png` is a deterministic, locally generated 320 × 180 RGB image
(teal rectangle and amber circle on a dark-blue background). It supplies the
real embedded image referenced by `deck_spec.json` and the read-back tests.
It is not a model-generated result or a substitute for an oracle.
