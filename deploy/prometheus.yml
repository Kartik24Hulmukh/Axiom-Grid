global: { scrape_interval: 15s, evaluation_interval: 15s }
rule_files: [/etc/prometheus/alerts.yml]
scrape_configs:
  - job_name: axiom-grid-api
    metrics_path: /metrics
    params: { format: [prometheus] }
    dns_sd_configs: [{ names: [api], type: A, port: 8765 }]
